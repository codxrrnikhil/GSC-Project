# Playwright (Chromium) for the crawler

After installing Python dependencies:

```bash
pip install -r requirements.txt
playwright install chromium
```

Without `playwright install chromium`, `CrawlerService` will fail when launching the browser.
